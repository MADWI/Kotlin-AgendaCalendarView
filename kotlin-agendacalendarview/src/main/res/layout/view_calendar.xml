<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <com.ognev.kotlin.agendacalendarview.calendar.DaysNamesHeaderView
        android:id="@+id/daysNamesHeaderView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <com.ognev.kotlin.agendacalendarview.calendar.weekslist.WeekListView
        android:id="@+id/weekListView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:cacheColorHint="@android:color/transparent"
        android:divider="@color/calendar_divider_color"
        android:dividerHeight="1dp"
        android:drawSelectorOnTop="false"
        android:listSelector="@android:color/transparent"
        android:overScrollMode="never"
        android:paddingBottom="5dp"
        android:paddingTop="6dp"
        android:scrollbars="none"
        app:layoutManager="LinearLayoutManager" />

</LinearLayout>
